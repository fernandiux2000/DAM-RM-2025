import { ColorHumedadDirective } from './color-humedad.directive';

describe('ColorHumedadDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHumedadDirective();
    expect(directive).toBeTruthy();
  });
});
